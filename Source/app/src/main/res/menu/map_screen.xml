<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.group2.whatsup.MapScreen" >
    <!-- android:icon="@drawable/ic_action_search"
        android:Title="@string/action_search"-->
    <item android:id="@+id/menu_add_event"
        android:title="@string/menu_add_event"
        android:icon="@+drawable/icon_add"
        android:showAsAction="ifRoom"
        />
    <item android:id="@+id/menu_refresh_list"
        android:title="@string/menu_refresh_list"
        android:icon="@+drawable/icon_refresh"
        android:showAsAction="ifRoom"
    />

</menu>
